# Standards: 0.1
---
# @var slack_user:description: User to run user-specific commands
# @var slack_user:value: slack_user | default(homebrew_user) | default(ansible_user_id)
# slack_user:

# @var slack_started:description: Restart app if already running
slack_started: True

...
